<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="SecretLead.ImportPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:SecretLead"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!--
        Import screen (README.md §3): two independent file pickers, no
        folder picker anywhere. Validation runs the instant both files are
        selected — the Import button's own enabled state IS the validation
        result. Per-mismatch detail lives in the shell's Status flyout
        (nav rail), not a box on this page.
    -->
    <Grid Padding="32" MaxWidth="520" HorizontalAlignment="Left" VerticalAlignment="Top">
        <StackPanel Spacing="20">
            <TextBlock Text="Import" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock
                Text="Pick a CSV file and the tag file that describes it. Every tag has to match a CSV column, both directions, before Import turns on — check the Status button for specifics."
                Style="{StaticResource BodyTextBlockStyle}"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                TextWrapping="Wrap" />

            <!-- CSV file -->
            <StackPanel Spacing="6">
                <TextBlock Text="CSV file" Style="{StaticResource BodyStrongTextBlockStyle}" />
                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="Choose file…" Command="{x:Bind ViewModel.PickCsvFileCommand}" />
                    <TextBlock
                        Grid.Column="1"
                        Text="{x:Bind ViewModel.CsvFileName, Mode=OneWay}"
                        VerticalAlignment="Center"
                        TextTrimming="CharacterEllipsis"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                </Grid>
            </StackPanel>

            <!-- Tag file -->
            <StackPanel Spacing="6">
                <TextBlock Text="Tag file (.md)" Style="{StaticResource BodyStrongTextBlockStyle}" />
                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="Choose file…" Command="{x:Bind ViewModel.PickTagFileCommand}" />
                    <TextBlock
                        Grid.Column="1"
                        Text="{x:Bind ViewModel.TagFileName, Mode=OneWay}"
                        VerticalAlignment="Center"
                        TextTrimming="CharacterEllipsis"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                </Grid>
            </StackPanel>

            <Button
                Content="Import"
                Command="{x:Bind ViewModel.ImportCommand}"
                IsEnabled="{x:Bind ViewModel.IsReadyToImport, Mode=OneWay}"
                Style="{StaticResource AccentButtonStyle}"
                HorizontalAlignment="Left"
                MinWidth="140" />

            <TextBlock
                Text="{x:Bind ViewModel.ResultMessage, Mode=OneWay}"
                Style="{StaticResource BodyTextBlockStyle}"
                TextWrapping="Wrap" />
        </StackPanel>
    </Grid>
</Page>
